/**
 * @fileoverview Shared type definitions for the Zillow ROI Analyzer.
 *
 * This package contains JSDoc type definitions shared across all units.
 * Import this package for IDE autocompletion and type checking.
 *
 * Usage:
 *   const types = require('@zillow-roi/types');
 *   // Or import individual type files for JSDoc references:
 *   // @typedef {import('@zillow-roi/types/src/property').PropertyListing} PropertyListing
 */

// Re-export all type modules (no runtime values, just for module resolution)
module.exports = {
  // Types are accessed via JSDoc @typedef imports, not runtime exports.
  // This file exists to make the package resolvable as a Node module.
};
