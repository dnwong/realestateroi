-- Sessions table is auto-created by connect-pg-simple when createTableIfMissing: true
-- This migration is a no-op placeholder for documentation purposes.
-- connect-pg-simple creates: sessions(sid VARCHAR PK, sess JSON, expire TIMESTAMPTZ)
SELECT 1;
